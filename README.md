# Supplement (anonymized review archive)

This is the anonymized supplement for *A Per-Component Diagnostic Protocol
for Neural HJB-PIDE Solvers under Control-Dependent Lévy Jumps*.

- See `SUPPLEMENT.md` for the full layout, the numeric-claim → result-JSON map,
  the rerun protocol, and the frozen-seed list.
- See `CONSISTENCY_LOG.md` for the term-by-term sweep that links every numeric
  string in the paper back to a result JSON, with rerunnable shell commands.

## Anonymity

No author or institution names appear in this archive. The public repository
URL will replace this archive at acceptance.
